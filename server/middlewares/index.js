/**
 * Module Dependencies
 */

import cors from './cors'
import ssl from './ssl'
import checkAccessToken from './check-access-token'
import validateEvent from './validate-event'

export default { ssl, cors, checkAccessToken, validateEvent }